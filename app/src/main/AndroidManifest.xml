<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nagajothy.smazee.sairamapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Homepage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".searchhome" />
        <activity android:name=".StudentListActivity" />
        <activity android:name=".AbsentActivity" />
        <activity android:name=".Yathrackone" />
        <activity android:name=".Yathracksplashscreen" />
        <activity android:name=".Yathracklogin" />
        <activity android:name=".yathrackalert"></activity>
    </application>

</manifest>